apiVersion: v1
kind: Secret
metadata:
  name: aws-creds
  namespace: upbound-system
type: Opaque
# For identity.source: WebIdentity with tokenConfig.source: Secret, the OIDC
# token bytes are read from the "token" key of the aws-creds credentials block.
# base64 (render reads `data`); decoded placeholder is "REPLACE_WITH_OIDC_JWT".
data:
  token: UkVQTEFDRV9XSVRIX09JRENfSldU
